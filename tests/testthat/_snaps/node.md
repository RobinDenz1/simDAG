# snapshot print node_td

    A DAG.node object specifying a single child node with:
      - name: 'A'
      - type: 'time_to_event'
      - no additional parameters
    This node may change over time.

